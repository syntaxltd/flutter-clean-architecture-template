name: presentation
description: A new Flutter package.
version: 0.0.0
homepage: https://github.com/Dennis-Mwea/flutter-clean-architecture-template
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  data:
    path: ../data
  domain:
    path: ../domain
  get_it: ^7.6.0
  injectable: ^2.1.1
  auto_route: ^7.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.2
  auto_route_generator: ^7.0.0
  injectable_generator: ^2.1.5

flutter:
